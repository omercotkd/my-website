## A cv like website built with React and MUI

### Before every push to main run:

```bash
npm run lint
```

fix any issues if there are any.


and make sure to test if the build works:

```bash
npm run build
```

